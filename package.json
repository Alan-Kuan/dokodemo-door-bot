{
  "name": "dokokemo-door-bot",
  "version": "1.1.0",
  "author": "Alan-Kuan",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "axios": "^1.12.0",
    "mongoose": "^8.15.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@jazim/mock-mongoose": "^1.0.1",
    "@telegraf/types": "^7.1.0",
    "@types/node": "^22.15.27",
    "@vercel/node": "^5.2.0",
    "esbuild": "^0.26.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "esbuild api/**/*.ts lib/**/*.ts --minify --outdir=dist --format=esm --platform=node --target=node22 --tree-shaking=true --define:import.meta.vitest=undefined",
    "type-check": "tsc --noEmit",
    "test": "vitest --watch=false"
  },
  "imports": {
    "#*": [
      "./lib/*"
    ]
  }
}
